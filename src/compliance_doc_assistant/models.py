# SQLModel table models and their *Create/*Read DTOs live here, colocated,
# following the same convention as evalops-dashboard's models.py.
#
# No tables yet — Milestone 2 (auth) adds the first ones (User, AuthSession).
